Build the ownership outline for a model viewer using `@playcanvas/web-components`. Place assets,
scene elements, and a GLB model declaratively, then access the Engine application for one feature
that has no declarative element. Explain when that access is safe.
