An `@playcanvas/react` project loads a GLB containing several clips. Show the safe component and hook
ownership pattern, explain whether the package's `Anim` component can select those clips by name,
and choose a reliable setup for idle, movement, and action transitions.
