Write the Direct PlayCanvas Engine transform hierarchy for a repeatedly instantiated GLB whose
tuning record contains `scale`, `y`, `yaw`, and a non-zero `center`. The returned root must represent
the gameplay position and heading, and yaw must not rotate the authored pivot offset a second time.
